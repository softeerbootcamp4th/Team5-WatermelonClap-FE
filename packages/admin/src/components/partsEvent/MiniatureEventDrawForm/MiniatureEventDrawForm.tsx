import { RewardInfo } from "../RewardInfo";

export const MiniatureEventDrawFrom = () => {
  return (
    <div>
      <RewardInfo
        rank={1}
        winnerCount={10}
        reward="커스텀된 아반떼 N 미니어처"
      />
    </div>
  );
};
